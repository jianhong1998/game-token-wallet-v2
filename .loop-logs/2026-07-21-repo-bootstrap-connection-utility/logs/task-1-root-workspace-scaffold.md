# Task 1 Log: Root workspace scaffold

## Task Context

### Plan Section
## Task 1: Root workspace scaffold

**Files:**
- Create: `pnpm-workspace.yaml`
- Create: `package.json`
- Create: `tsconfig.base.json`
- Create: `eslint.config.mjs`
- Create: `.prettierrc.json`
- Create: `.gitignore`
- Modify: `Justfile`

**Interfaces:**
- Produces: root `pnpm` scripts `lint`, `typecheck`, `test`, `build` — each fans out to every workspace package via `pnpm -r --if-present run <script>`. Later tasks rely on these names existing verbatim.

- [ ] **Step 1: Create the pnpm workspace manifest**

`pnpm-workspace.yaml`:
```yaml
packages:
  - "apps/*"
  - "packages/*"
```

- [ ] **Step 2: Create the root package.json**

`package.json`:
```json
{
  "name": "game-token-wallet-v2",
  "private": true,
  "packageManager": "pnpm@11.15.1",
  "engines": {
    "node": ">=24.18.0"
  },
  "scripts": {
    "lint": "pnpm -r --if-present run lint",
    "typecheck": "pnpm -r --if-present run typecheck",
    "test": "pnpm -r --if-present run test",
    "build": "pnpm -r --if-present run build",
    "codegen": "pnpm --filter on-chain-program run build && pnpm --filter on-chain-client run codegen"
  },
  "devDependencies": {
    "eslint": "^9.19.0",
    "prettier": "^3.4.2",
    "typescript": "^5.7.3"
  }
}
```

- [ ] **Step 3: Create the shared base tsconfig**

`tsconfig.base.json`:
```json
{
  "compilerOptions": {
    "target": "ES2022",
    "lib": ["ES2022"],
    "module": "ESNext",
    "moduleResolution": "Bundler",
    "esModuleInterop": true,
    "resolveJsonModule": true,
    "strict": true,
    "skipLibCheck": true,
    "forceConsistentCasingInFileNames": true,
    "isolatedModules": true
  }
}
```

- [ ] **Step 4: Create the root ESLint flat config**

`eslint.config.mjs`:
```javascript
import js from "@eslint/js";
import tseslint from "typescript-eslint";

export default tseslint.config(
  js.configs.recommended,
  ...tseslint.configs.recommended,
  {
    ignores: ["**/dist/**", "**/.next/**", "**/target/**", "**/generated/**"],
  },
);
```

- [ ] **Step 5: Install the ESLint config's own dependencies**

Run: `pnpm add -D -w @eslint/js typescript-eslint`
Expected: adds both packages to root `devDependencies` and creates `pnpm-lock.yaml`.

- [ ] **Step 6: Create Prettier config**

`.prettierrc.json`:
```json
{
  "semi": true,
  "singleQuote": false,
  "trailingComma": "all",
  "printWidth": 100
}
```

- [ ] **Step 7: Create root .gitignore**

`.gitignore`:
```
node_modules/
.next/
dist/
target/
!apps/on-chain-program/target/idl/
.env
.env.local
*.log
.DS_Store
```

- [ ] **Step 8: Extend the Justfile with lint/typecheck targets**

Modify `Justfile` — add after the existing `init`/`help` recipes:
```
[group: 'CI']
lint:
  @pnpm lint

[group: 'CI']
typecheck:
  @pnpm typecheck
```

- [ ] **Step 9: Verify the workspace installs cleanly**

Run: `pnpm install`
Expected: exits 0, creates `node_modules/` and `pnpm-lock.yaml`, no workspace packages to link yet (none created before Task 2).

- [ ] **Step 10: Commit**

```bash
git add pnpm-workspace.yaml package.json pnpm-lock.yaml tsconfig.base.json eslint.config.mjs .prettierrc.json .gitignore Justfile
git commit -m "chore: bootstrap pnpm workspace scaffold"
```

---

## Attempt 1 — 2026-07-21T10:57:11Z

### Implementation Plan
- Create Steps 1-4, 6 config files fresh (`pnpm-workspace.yaml`, `package.json`, `tsconfig.base.json`, `eslint.config.mjs`, `.prettierrc.json`) with exact contents from the plan
- Merge Step 7's required `.gitignore` entries into the existing (non-empty) `.gitignore` rather than clobbering it
- Append Step 8's `lint`/`typecheck` Justfile recipes after the existing `init`/`help` recipes
- Run Step 5 (`pnpm add -D -w @eslint/js typescript-eslint`) and Step 9 (`pnpm install`) with the corepack-pinned pnpm on PATH, confirm both exit 0
- Stage exactly the 8 files listed in Step 10 and commit

### Files Changed
- created `pnpm-workspace.yaml` — workspace package globs (`apps/*`, `packages/*`)
- created `package.json` — root manifest, pinned packageManager/engines, `lint`/`typecheck`/`test`/`build`/`codegen` scripts
- created `tsconfig.base.json` — shared strict TS base config
- created `eslint.config.mjs` — root flat ESLint config (js + typescript-eslint recommended)
- created `.prettierrc.json` — root Prettier config
- created `pnpm-lock.yaml` — generated by `pnpm add`/`pnpm install`
- modified `.gitignore` — merged plan's required entries (`.next/`, `dist/`, `target/`, `!apps/on-chain-program/target/idl/`, `*.log`) into existing file; `.env`, `.env*.local` (covers `.env.local`), `.DS_Store`, `node_modules` were already present so left as-is, no duplication
- modified `Justfile` — appended `lint`/`typecheck` recipes under `[group: 'CI']` after existing `init`/`help` recipes

### New Tests
(none — pure scaffolding task, no test-writing step per task instructions)

### Key Decisions
- Kept the pre-existing `.gitignore` entries (`node_modules` without trailing slash, `.env*.local` glob) instead of replacing them with the plan's literal lines, since they already achieve equivalent or broader coverage than the plan's `node_modules/`/`.env.local` — avoided duplicate/conflicting patterns
- Left `docker-compose.yml` (empty, Task 9's responsibility) untouched — out of scope for Task 1 per orchestrator instructions

### Lint Output
n/a — no lint config target exists to run yet (no workspace packages); Step 9's `pnpm install` is this task's verification gate per task instructions

### Test Output
n/a — no test-writing step for this task (pure scaffolding); verification gate is `pnpm install` exiting 0 (see below)

### Verification Gate (pnpm install)
```
$ pnpm --version
11.15.1
$ pnpm install
ok
EXIT: 0
```
`node_modules/` and `pnpm-lock.yaml` present after install; no workspace packages linked (expected — none exist before Task 2).

### Commit
`7cd8125`

### Outcome: success
