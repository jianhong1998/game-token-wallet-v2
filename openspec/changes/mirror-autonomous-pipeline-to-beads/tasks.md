## 1. Validate the formula primitives (spike — reconfirm before full authoring)

- [x] 1.1 Confirm reparenting: `bd mol bond <formula> <ticket-id> --ref pipeline` (not `bd mol pour --parent`, which doesn't exist) nests the formula under the ticket's ID namespace (`<ticket>.pipeline`, ...), renders as a tree under the ticket via `bd children <ticket>`, and leaves the ticket's `external_ref`/`labels`/`issue_type` untouched (`bd show <ticket> --json`). See design.md Decision 2.
- [x] 1.2 Confirm dynamic fan-out: for a spawner step inside a poured/bonded molecule, `bd mol bond <child-formula> <spawner-step-id> --ref <name> [--var k=v]` creates a real dotted-ID child (`<spawner>.<name>`), visible in `bd children`/`bd mol show`. Confirm this must be issued manually per item — `on_complete.for_each` declared in the formula does not auto-fire when the spawner step closes (setting `metadata.output.items` then `bd close --continue` produces zero automatic children). See design.md Decision 1.
- [x] 1.3 Confirm `LoopSpec` (`until`/`max`) is cook-time sugar only: pouring materializes iteration 1 (labelled `loop:{...}`), but closing its body step does not spawn iteration 2, and there is no bd-enforced cap. Confirm the orchestrator must count iterations and stop itself.
- [x] 1.4 Confirm a `type: human` `Gate` step behaves as documented: pouring creates a real blocking `gate` issue, `bd blocked` lists the gated step, and `bd gate resolve <gate-id>` unblocks it. (Fully confirmed working as-is in the initial spike — no correction needed, just a quick reconfirm.)
- [x] 1.5 Delete the spike formula(s) and scratch issues once reconfirmed (`bd delete <ids> --force`). If a following `bd` command warns `.beads/issues.jsonl contains N JSONL-only issue record(s) absent from the local Dolt store, refusing to overwrite` — Dolt already dropped them but the JSONL export didn't — run `git checkout -- .beads/issues.jsonl` to discard the stale export (Dolt is the source of truth; the JSONL file is disposable and regenerates clean on the next successful auto-export).

## 2. bd status configuration

- [x] 2.1 Run `bd config set status.custom "failed:wip,pending-human-review:wip"` in this repo.
- [x] 2.2 Confirm `bd statuses` lists both new values with category `wip`.

## 3. Author the pipeline formulas

- [x] 3.1 Create `.beads/formulas/autonomous-pipeline.formula.toml` — the fixed skeleton bonded once per ticket: a `verify_review_loop` step and a `finalize` step (`needs: ["verify_review_loop"]`). Do **not** give `verify_review_loop` a formula-declared `gate` field — that would create the human Gate unconditionally at bond time and block clean autonomous runs from ever closing it; the Gate is created on demand instead (see §4.3). Do **not** use `LoopSpec` on `verify_review_loop` either — §1.3 confirmed bd doesn't enforce `until`/`max`, so a plain step is simpler and equally correct; the orchestrator tracks iteration count itself via `.loop-logs/`.
- [x] 3.2 Create `.beads/formulas/autonomous-pipeline-task.formula.toml` — a single-step, reusable formula (one `{{task_name}}` var) representing one `task-N` child, meant to be bonded individually per plan task.
- [x] 3.3 Create `.beads/formulas/autonomous-pipeline-fix.formula.toml` — a single-step, reusable formula (one `{{finding_summary}}` var) representing one `fix-<issue-id>` child, meant to be bonded individually per actionable review finding.
- [x] 3.4 `bd formula show` each of the three and confirm structure matches `specs/beads-pipeline-mirroring/spec.md`'s requirements (skeleton = `verify_review_loop` + `finalize`, no `gate` field; task/fix children bondable individually, count determined at runtime, not fixed by the formula).
- [x] 3.5 Dry-run then real-run the full bond sequence against a disposable test issue (not a real ticket): `bd mol bond autonomous-pipeline <test-issue> --ref pipeline`, then `bd mol bond autonomous-pipeline-task <test-issue>.pipeline --ref task-1 --var task_name=...` (repeat for 2-3 fake tasks), then `bd mol bond autonomous-pipeline-fix <test-issue>.pipeline.verify_review_loop --ref iter-1-fix-1 --var finding_summary=...`. Confirm the resulting tree (`bd children <test-issue>`) matches design.md Decision 2's expected shape (ticket → `pipeline` → task-N/verify_review_loop/finalize, with fix-N nested under verify_review_loop), and that the test issue's `external_ref`/`labels` survive untouched.

## 4. Write the orchestrator-facing mirroring doc

- [x] 4.1 Create `docs/beads/autonomous-pipeline-mirroring.md`, naming the vendored plugin version it's written against (`autonomous-development-plugin` `0.4.2`) per design.md's plugin-version-drift risk mitigation, and naming the bd version it's written against (`bd 1.2.1`) since the exact bond/reparent mechanics are version-specific (design.md Decisions 1 & 2).
- [x] 4.2 Document the Stage 0/1 hook: once tasks are parsed (immediately after `.loop-logs/<id>/tasks/<task-id>.json` files are first written, stage-impl.md Step 0.5), run `bd mol bond autonomous-pipeline <ticket-id> --ref pipeline` once, then one `bd mol bond autonomous-pipeline-task <ticket-id>.pipeline --ref task-N --var task_name=...` per parsed task. Document each task-N status transition as a `bd update <ticket-id>.pipeline.task-N --status ...` call, phrased against stage *behavior* (e.g. "after a task's TDD loop reaches Outcome: success/failed"), not line numbers.
- [x] 4.3 Document the Stage 2/3 hook: for each review round, bond one `fix-<issue-id>` child per actionable finding via `bd mol bond autonomous-pipeline-fix <ticket-id>.pipeline.verify_review_loop --ref iter-N-fix-M --var ...`; state explicitly that the orchestrator (not bd) counts rounds and enforces the 5-iteration cap. Document the `failed:wip` + `bd comments add` on cap exhaustion. Document the human-in-loop pause as an on-demand `bd gate create --type=human --blocks <ticket-id>.pipeline.verify_review_loop --reason "<checklist summary>"`, run only when stage-verify.md's `awaiting_human` handoff actually occurs (`interaction_mode == human-in-loop`, non-empty `blocked`) — not created upfront by the skeleton formula (see §3.1) — and closed via `bd gate resolve <gate-id>` when the human replies `continue`.
- [x] 4.4 Document the Stage 4 hook: setting the epic's terminal status (`pending-human-review:wip` vs `failed:wip`) at stage-final.md's terminal step (commit in `autonomous` mode, unstaged-handoff print in `human-in-loop` mode) via `bd update <ticket-id> --status ...`, explicitly never closing the epic.
- [x] 4.5 State explicitly in the doc: every command here is additive — a `bd` call failing/being unavailable must never block or alter the orchestrator's own `.loop-logs/` writes (spec requirement "bd is a mirror, not the pipeline's source of truth"). If a bond call fails mid-fan-out, log it and continue; do not retry-loop.

## 5. Wire the doc into the repo's always-loaded context

- [x] 5.1 Add one pointer line in `CLAUDE.md`'s existing Beads Issue Tracker section linking to `docs/beads/autonomous-pipeline-mirroring.md`, matching the existing `docs/beads/instruction.md` link's style.
- [x] 5.2 Edit `.claude/skills/plan-implementation/SKILL.md` Step 6 to name `pending-human-review`/`failed` explicitly instead of the current "update the ticket's status."

## 6. End-to-end validation

**Deferred to manual validation on the next real `autonomous-feature-development` run** — not exercised in this session (spawning worktree agents / running a live TDD-verify-review loop for validation-only purposes was judged too heavy/side-effectful to manufacture here; sections 1–5 already produced and spike-validated the actual deliverable). Re-open and check off individually the first time a real ticket runs through the pipeline with this mirroring doc active.

- [ ] 6.1 Run the full pipeline (`autonomous-feature-development`) against a small real or throwaway plan/spec pair with the mirroring doc active, and confirm: `task-N` children are bonded live per parsed task, at least one review round's `fix-<issue-id>` children are correctly bonded under `verify_review_loop` (with the orchestrator's own count governing the loop, not a bd-native mechanism), and the epic lands on `pending-human-review:wip` (clean run) with the ticket issue's original `external_ref`/`labels` intact.
- [ ] 6.2 Force a task failure (or simulate one) and confirm the corresponding `<ticket-id>.pipeline.task-N` child is set to `failed:wip` with a root-cause-only `bd comments add` entry, and the epic ends at `failed:wip` rather than `pending-human-review:wip`.
- [ ] 6.3 Confirm `.loop-logs/<id>/tasks/*.json` content is unaffected/unchanged by any of the above (bd mirroring made no changes to the orchestrator's own bookkeeping format or content).
