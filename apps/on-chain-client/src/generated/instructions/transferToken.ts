/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  addDecoderSizePrefix,
  addEncoderSizePrefix,
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  getU32Decoder,
  getU32Encoder,
  getU64Decoder,
  getU64Encoder,
  getUtf8Decoder,
  getUtf8Encoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type Codec,
  type Decoder,
  type Encoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import {
  findGamePda,
  findMintPda,
  findRecipientPda,
  findSenderPda,
} from "../pdas";
import { GAME_TOKEN_WALLET_PROGRAM_ADDRESS } from "../programs";
import {
  expectAddress,
  expectSome,
  getAccountMetaFactory,
  type ResolvedAccount,
} from "../shared";

export const TRANSFER_TOKEN_DISCRIMINATOR = new Uint8Array([
  219, 17, 122, 53, 237, 171, 232, 222,
]);

export function getTransferTokenDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    TRANSFER_TOKEN_DISCRIMINATOR,
  );
}

export type TransferTokenInstruction<
  TProgram extends string = typeof GAME_TOKEN_WALLET_PROGRAM_ADDRESS,
  TAccountAdmin extends string | AccountMeta<string> = string,
  TAccountSender extends string | AccountMeta<string> = string,
  TAccountRecipient extends string | AccountMeta<string> = string,
  TAccountGame extends string | AccountMeta<string> = string,
  TAccountMint extends string | AccountMeta<string> = string,
  TAccountSenderAta extends string | AccountMeta<string> = string,
  TAccountRecipientAta extends string | AccountMeta<string> = string,
  TAccountTokenProgram extends string | AccountMeta<string> =
    "TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA",
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountAdmin extends string
        ? WritableSignerAccount<TAccountAdmin> &
            AccountSignerMeta<TAccountAdmin>
        : TAccountAdmin,
      TAccountSender extends string
        ? ReadonlyAccount<TAccountSender>
        : TAccountSender,
      TAccountRecipient extends string
        ? ReadonlyAccount<TAccountRecipient>
        : TAccountRecipient,
      TAccountGame extends string
        ? ReadonlyAccount<TAccountGame>
        : TAccountGame,
      TAccountMint extends string
        ? ReadonlyAccount<TAccountMint>
        : TAccountMint,
      TAccountSenderAta extends string
        ? WritableAccount<TAccountSenderAta>
        : TAccountSenderAta,
      TAccountRecipientAta extends string
        ? WritableAccount<TAccountRecipientAta>
        : TAccountRecipientAta,
      TAccountTokenProgram extends string
        ? ReadonlyAccount<TAccountTokenProgram>
        : TAccountTokenProgram,
      ...TRemainingAccounts,
    ]
  >;

export type TransferTokenInstructionData = {
  discriminator: ReadonlyUint8Array;
  gameId: ReadonlyUint8Array;
  senderUsername: string;
  recipientUsername: string;
  amount: bigint;
};

export type TransferTokenInstructionDataArgs = {
  gameId: ReadonlyUint8Array;
  senderUsername: string;
  recipientUsername: string;
  amount: number | bigint;
};

export function getTransferTokenInstructionDataEncoder(): Encoder<TransferTokenInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["gameId", fixEncoderSize(getBytesEncoder(), 16)],
      [
        "senderUsername",
        addEncoderSizePrefix(getUtf8Encoder(), getU32Encoder()),
      ],
      [
        "recipientUsername",
        addEncoderSizePrefix(getUtf8Encoder(), getU32Encoder()),
      ],
      ["amount", getU64Encoder()],
    ]),
    (value) => ({ ...value, discriminator: TRANSFER_TOKEN_DISCRIMINATOR }),
  );
}

export function getTransferTokenInstructionDataDecoder(): Decoder<TransferTokenInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["gameId", fixDecoderSize(getBytesDecoder(), 16)],
    ["senderUsername", addDecoderSizePrefix(getUtf8Decoder(), getU32Decoder())],
    [
      "recipientUsername",
      addDecoderSizePrefix(getUtf8Decoder(), getU32Decoder()),
    ],
    ["amount", getU64Decoder()],
  ]);
}

export function getTransferTokenInstructionDataCodec(): Codec<
  TransferTokenInstructionDataArgs,
  TransferTokenInstructionData
> {
  return combineCodec(
    getTransferTokenInstructionDataEncoder(),
    getTransferTokenInstructionDataDecoder(),
  );
}

export type TransferTokenAsyncInput<
  TAccountAdmin extends string = string,
  TAccountSender extends string = string,
  TAccountRecipient extends string = string,
  TAccountGame extends string = string,
  TAccountMint extends string = string,
  TAccountSenderAta extends string = string,
  TAccountRecipientAta extends string = string,
  TAccountTokenProgram extends string = string,
> = {
  admin: TransactionSigner<TAccountAdmin>;
  sender?: Address<TAccountSender>;
  recipient?: Address<TAccountRecipient>;
  game?: Address<TAccountGame>;
  mint?: Address<TAccountMint>;
  /**
   * address is validated against the deterministic ATA derivation for
   * `(sender, mint)` in the handler. Unlike `recipient_ata` below, its
   * initialized state isn't checked explicitly — it must already hold a
   * balance for the CPI to succeed, so the SPL CPI's own rejection is the
   * guarantee (see design.md decision 1's account table).
   */
  senderAta: Address<TAccountSenderAta>;
  /**
   * address is validated against the deterministic ATA derivation for
   * `(recipient, mint)` in the handler, and its initialized state is
   * checked explicitly there too — transferring never auto-joins a
   * player, mirroring `MintToPlayer`'s own `player_ata` validation.
   */
  recipientAta: Address<TAccountRecipientAta>;
  tokenProgram?: Address<TAccountTokenProgram>;
  gameId: TransferTokenInstructionDataArgs["gameId"];
  senderUsername: TransferTokenInstructionDataArgs["senderUsername"];
  recipientUsername: TransferTokenInstructionDataArgs["recipientUsername"];
  amount: TransferTokenInstructionDataArgs["amount"];
};

export async function getTransferTokenInstructionAsync<
  TAccountAdmin extends string,
  TAccountSender extends string,
  TAccountRecipient extends string,
  TAccountGame extends string,
  TAccountMint extends string,
  TAccountSenderAta extends string,
  TAccountRecipientAta extends string,
  TAccountTokenProgram extends string,
  TProgramAddress extends Address = typeof GAME_TOKEN_WALLET_PROGRAM_ADDRESS,
>(
  input: TransferTokenAsyncInput<
    TAccountAdmin,
    TAccountSender,
    TAccountRecipient,
    TAccountGame,
    TAccountMint,
    TAccountSenderAta,
    TAccountRecipientAta,
    TAccountTokenProgram
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  TransferTokenInstruction<
    TProgramAddress,
    TAccountAdmin,
    TAccountSender,
    TAccountRecipient,
    TAccountGame,
    TAccountMint,
    TAccountSenderAta,
    TAccountRecipientAta,
    TAccountTokenProgram
  >
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? GAME_TOKEN_WALLET_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    admin: { value: input.admin ?? null, isWritable: true },
    sender: { value: input.sender ?? null, isWritable: false },
    recipient: { value: input.recipient ?? null, isWritable: false },
    game: { value: input.game ?? null, isWritable: false },
    mint: { value: input.mint ?? null, isWritable: false },
    senderAta: { value: input.senderAta ?? null, isWritable: true },
    recipientAta: { value: input.recipientAta ?? null, isWritable: true },
    tokenProgram: { value: input.tokenProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.sender.value) {
    accounts.sender.value = await findSenderPda({
      senderUsername: expectSome(args.senderUsername),
      admin: expectAddress(accounts.admin.value),
    });
  }
  if (!accounts.recipient.value) {
    accounts.recipient.value = await findRecipientPda({
      recipientUsername: expectSome(args.recipientUsername),
      admin: expectAddress(accounts.admin.value),
    });
  }
  if (!accounts.game.value) {
    accounts.game.value = await findGamePda({
      gameId: expectSome(args.gameId),
    });
  }
  if (!accounts.mint.value) {
    accounts.mint.value = await findMintPda({
      game: expectAddress(accounts.game.value),
    });
  }
  if (!accounts.tokenProgram.value) {
    accounts.tokenProgram.value =
      "TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA" as Address<"TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.admin),
      getAccountMeta(accounts.sender),
      getAccountMeta(accounts.recipient),
      getAccountMeta(accounts.game),
      getAccountMeta(accounts.mint),
      getAccountMeta(accounts.senderAta),
      getAccountMeta(accounts.recipientAta),
      getAccountMeta(accounts.tokenProgram),
    ],
    data: getTransferTokenInstructionDataEncoder().encode(
      args as TransferTokenInstructionDataArgs,
    ),
    programAddress,
  } as TransferTokenInstruction<
    TProgramAddress,
    TAccountAdmin,
    TAccountSender,
    TAccountRecipient,
    TAccountGame,
    TAccountMint,
    TAccountSenderAta,
    TAccountRecipientAta,
    TAccountTokenProgram
  >);
}

export type TransferTokenInput<
  TAccountAdmin extends string = string,
  TAccountSender extends string = string,
  TAccountRecipient extends string = string,
  TAccountGame extends string = string,
  TAccountMint extends string = string,
  TAccountSenderAta extends string = string,
  TAccountRecipientAta extends string = string,
  TAccountTokenProgram extends string = string,
> = {
  admin: TransactionSigner<TAccountAdmin>;
  sender: Address<TAccountSender>;
  recipient: Address<TAccountRecipient>;
  game: Address<TAccountGame>;
  mint: Address<TAccountMint>;
  /**
   * address is validated against the deterministic ATA derivation for
   * `(sender, mint)` in the handler. Unlike `recipient_ata` below, its
   * initialized state isn't checked explicitly — it must already hold a
   * balance for the CPI to succeed, so the SPL CPI's own rejection is the
   * guarantee (see design.md decision 1's account table).
   */
  senderAta: Address<TAccountSenderAta>;
  /**
   * address is validated against the deterministic ATA derivation for
   * `(recipient, mint)` in the handler, and its initialized state is
   * checked explicitly there too — transferring never auto-joins a
   * player, mirroring `MintToPlayer`'s own `player_ata` validation.
   */
  recipientAta: Address<TAccountRecipientAta>;
  tokenProgram?: Address<TAccountTokenProgram>;
  gameId: TransferTokenInstructionDataArgs["gameId"];
  senderUsername: TransferTokenInstructionDataArgs["senderUsername"];
  recipientUsername: TransferTokenInstructionDataArgs["recipientUsername"];
  amount: TransferTokenInstructionDataArgs["amount"];
};

export function getTransferTokenInstruction<
  TAccountAdmin extends string,
  TAccountSender extends string,
  TAccountRecipient extends string,
  TAccountGame extends string,
  TAccountMint extends string,
  TAccountSenderAta extends string,
  TAccountRecipientAta extends string,
  TAccountTokenProgram extends string,
  TProgramAddress extends Address = typeof GAME_TOKEN_WALLET_PROGRAM_ADDRESS,
>(
  input: TransferTokenInput<
    TAccountAdmin,
    TAccountSender,
    TAccountRecipient,
    TAccountGame,
    TAccountMint,
    TAccountSenderAta,
    TAccountRecipientAta,
    TAccountTokenProgram
  >,
  config?: { programAddress?: TProgramAddress },
): TransferTokenInstruction<
  TProgramAddress,
  TAccountAdmin,
  TAccountSender,
  TAccountRecipient,
  TAccountGame,
  TAccountMint,
  TAccountSenderAta,
  TAccountRecipientAta,
  TAccountTokenProgram
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? GAME_TOKEN_WALLET_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    admin: { value: input.admin ?? null, isWritable: true },
    sender: { value: input.sender ?? null, isWritable: false },
    recipient: { value: input.recipient ?? null, isWritable: false },
    game: { value: input.game ?? null, isWritable: false },
    mint: { value: input.mint ?? null, isWritable: false },
    senderAta: { value: input.senderAta ?? null, isWritable: true },
    recipientAta: { value: input.recipientAta ?? null, isWritable: true },
    tokenProgram: { value: input.tokenProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.tokenProgram.value) {
    accounts.tokenProgram.value =
      "TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA" as Address<"TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.admin),
      getAccountMeta(accounts.sender),
      getAccountMeta(accounts.recipient),
      getAccountMeta(accounts.game),
      getAccountMeta(accounts.mint),
      getAccountMeta(accounts.senderAta),
      getAccountMeta(accounts.recipientAta),
      getAccountMeta(accounts.tokenProgram),
    ],
    data: getTransferTokenInstructionDataEncoder().encode(
      args as TransferTokenInstructionDataArgs,
    ),
    programAddress,
  } as TransferTokenInstruction<
    TProgramAddress,
    TAccountAdmin,
    TAccountSender,
    TAccountRecipient,
    TAccountGame,
    TAccountMint,
    TAccountSenderAta,
    TAccountRecipientAta,
    TAccountTokenProgram
  >);
}

export type ParsedTransferTokenInstruction<
  TProgram extends string = typeof GAME_TOKEN_WALLET_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    admin: TAccountMetas[0];
    sender: TAccountMetas[1];
    recipient: TAccountMetas[2];
    game: TAccountMetas[3];
    mint: TAccountMetas[4];
    /**
     * address is validated against the deterministic ATA derivation for
     * `(sender, mint)` in the handler. Unlike `recipient_ata` below, its
     * initialized state isn't checked explicitly — it must already hold a
     * balance for the CPI to succeed, so the SPL CPI's own rejection is the
     * guarantee (see design.md decision 1's account table).
     */
    senderAta: TAccountMetas[5];
    /**
     * address is validated against the deterministic ATA derivation for
     * `(recipient, mint)` in the handler, and its initialized state is
     * checked explicitly there too — transferring never auto-joins a
     * player, mirroring `MintToPlayer`'s own `player_ata` validation.
     */
    recipientAta: TAccountMetas[6];
    tokenProgram: TAccountMetas[7];
  };
  data: TransferTokenInstructionData;
};

export function parseTransferTokenInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedTransferTokenInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 8) {
    // TODO: Coded error.
    throw new Error("Not enough accounts");
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      admin: getNextAccount(),
      sender: getNextAccount(),
      recipient: getNextAccount(),
      game: getNextAccount(),
      mint: getNextAccount(),
      senderAta: getNextAccount(),
      recipientAta: getNextAccount(),
      tokenProgram: getNextAccount(),
    },
    data: getTransferTokenInstructionDataDecoder().decode(instruction.data),
  };
}
