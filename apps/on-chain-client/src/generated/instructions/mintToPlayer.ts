/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  addDecoderSizePrefix,
  addEncoderSizePrefix,
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  getU32Decoder,
  getU32Encoder,
  getU64Decoder,
  getU64Encoder,
  getUtf8Decoder,
  getUtf8Encoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type Codec,
  type Decoder,
  type Encoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import {
  findGamePda,
  findMintPda,
  findPlayerUserPda,
  findUserPda,
} from "../pdas";
import { GAME_TOKEN_WALLET_PROGRAM_ADDRESS } from "../programs";
import {
  expectAddress,
  expectSome,
  getAccountMetaFactory,
  type ResolvedAccount,
} from "../shared";

export const MINT_TO_PLAYER_DISCRIMINATOR = new Uint8Array([
  137, 49, 202, 132, 62, 0, 217, 29,
]);

export function getMintToPlayerDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    MINT_TO_PLAYER_DISCRIMINATOR,
  );
}

export type MintToPlayerInstruction<
  TProgram extends string = typeof GAME_TOKEN_WALLET_PROGRAM_ADDRESS,
  TAccountAdmin extends string | AccountMeta<string> = string,
  TAccountUser extends string | AccountMeta<string> = string,
  TAccountPlayerUser extends string | AccountMeta<string> = string,
  TAccountGame extends string | AccountMeta<string> = string,
  TAccountMint extends string | AccountMeta<string> = string,
  TAccountPlayerAta extends string | AccountMeta<string> = string,
  TAccountTokenProgram extends string | AccountMeta<string> =
    "TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA",
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountAdmin extends string
        ? WritableSignerAccount<TAccountAdmin> &
            AccountSignerMeta<TAccountAdmin>
        : TAccountAdmin,
      TAccountUser extends string
        ? ReadonlyAccount<TAccountUser>
        : TAccountUser,
      TAccountPlayerUser extends string
        ? ReadonlyAccount<TAccountPlayerUser>
        : TAccountPlayerUser,
      TAccountGame extends string
        ? ReadonlyAccount<TAccountGame>
        : TAccountGame,
      TAccountMint extends string
        ? WritableAccount<TAccountMint>
        : TAccountMint,
      TAccountPlayerAta extends string
        ? WritableAccount<TAccountPlayerAta>
        : TAccountPlayerAta,
      TAccountTokenProgram extends string
        ? ReadonlyAccount<TAccountTokenProgram>
        : TAccountTokenProgram,
      ...TRemainingAccounts,
    ]
  >;

export type MintToPlayerInstructionData = {
  discriminator: ReadonlyUint8Array;
  gameId: ReadonlyUint8Array;
  username: string;
  playerUsername: string;
  amount: bigint;
};

export type MintToPlayerInstructionDataArgs = {
  gameId: ReadonlyUint8Array;
  username: string;
  playerUsername: string;
  amount: number | bigint;
};

export function getMintToPlayerInstructionDataEncoder(): Encoder<MintToPlayerInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["gameId", fixEncoderSize(getBytesEncoder(), 16)],
      ["username", addEncoderSizePrefix(getUtf8Encoder(), getU32Encoder())],
      [
        "playerUsername",
        addEncoderSizePrefix(getUtf8Encoder(), getU32Encoder()),
      ],
      ["amount", getU64Encoder()],
    ]),
    (value) => ({ ...value, discriminator: MINT_TO_PLAYER_DISCRIMINATOR }),
  );
}

export function getMintToPlayerInstructionDataDecoder(): Decoder<MintToPlayerInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["gameId", fixDecoderSize(getBytesDecoder(), 16)],
    ["username", addDecoderSizePrefix(getUtf8Decoder(), getU32Decoder())],
    ["playerUsername", addDecoderSizePrefix(getUtf8Decoder(), getU32Decoder())],
    ["amount", getU64Decoder()],
  ]);
}

export function getMintToPlayerInstructionDataCodec(): Codec<
  MintToPlayerInstructionDataArgs,
  MintToPlayerInstructionData
> {
  return combineCodec(
    getMintToPlayerInstructionDataEncoder(),
    getMintToPlayerInstructionDataDecoder(),
  );
}

export type MintToPlayerAsyncInput<
  TAccountAdmin extends string = string,
  TAccountUser extends string = string,
  TAccountPlayerUser extends string = string,
  TAccountGame extends string = string,
  TAccountMint extends string = string,
  TAccountPlayerAta extends string = string,
  TAccountTokenProgram extends string = string,
> = {
  admin: TransactionSigner<TAccountAdmin>;
  user?: Address<TAccountUser>;
  playerUser?: Address<TAccountPlayerUser>;
  game?: Address<TAccountGame>;
  mint?: Address<TAccountMint>;
  /**
   * `mint`. Its address is validated against the deterministic ATA
   * derivation for `(player_user, mint)` in the handler, and its
   * initialized state is checked explicitly there too — it must already
   * exist, since depositing never auto-joins a player — mirroring
   * `JoinGame`'s own `player_ata` validation approach.
   */
  playerAta: Address<TAccountPlayerAta>;
  tokenProgram?: Address<TAccountTokenProgram>;
  gameId: MintToPlayerInstructionDataArgs["gameId"];
  username: MintToPlayerInstructionDataArgs["username"];
  playerUsername: MintToPlayerInstructionDataArgs["playerUsername"];
  amount: MintToPlayerInstructionDataArgs["amount"];
};

export async function getMintToPlayerInstructionAsync<
  TAccountAdmin extends string,
  TAccountUser extends string,
  TAccountPlayerUser extends string,
  TAccountGame extends string,
  TAccountMint extends string,
  TAccountPlayerAta extends string,
  TAccountTokenProgram extends string,
  TProgramAddress extends Address = typeof GAME_TOKEN_WALLET_PROGRAM_ADDRESS,
>(
  input: MintToPlayerAsyncInput<
    TAccountAdmin,
    TAccountUser,
    TAccountPlayerUser,
    TAccountGame,
    TAccountMint,
    TAccountPlayerAta,
    TAccountTokenProgram
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  MintToPlayerInstruction<
    TProgramAddress,
    TAccountAdmin,
    TAccountUser,
    TAccountPlayerUser,
    TAccountGame,
    TAccountMint,
    TAccountPlayerAta,
    TAccountTokenProgram
  >
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? GAME_TOKEN_WALLET_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    admin: { value: input.admin ?? null, isWritable: true },
    user: { value: input.user ?? null, isWritable: false },
    playerUser: { value: input.playerUser ?? null, isWritable: false },
    game: { value: input.game ?? null, isWritable: false },
    mint: { value: input.mint ?? null, isWritable: true },
    playerAta: { value: input.playerAta ?? null, isWritable: true },
    tokenProgram: { value: input.tokenProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.user.value) {
    accounts.user.value = await findUserPda({
      username: expectSome(args.username),
      admin: expectAddress(accounts.admin.value),
    });
  }
  if (!accounts.playerUser.value) {
    accounts.playerUser.value = await findPlayerUserPda({
      playerUsername: expectSome(args.playerUsername),
      admin: expectAddress(accounts.admin.value),
    });
  }
  if (!accounts.game.value) {
    accounts.game.value = await findGamePda({
      gameId: expectSome(args.gameId),
    });
  }
  if (!accounts.mint.value) {
    accounts.mint.value = await findMintPda({
      game: expectAddress(accounts.game.value),
    });
  }
  if (!accounts.tokenProgram.value) {
    accounts.tokenProgram.value =
      "TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA" as Address<"TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.admin),
      getAccountMeta(accounts.user),
      getAccountMeta(accounts.playerUser),
      getAccountMeta(accounts.game),
      getAccountMeta(accounts.mint),
      getAccountMeta(accounts.playerAta),
      getAccountMeta(accounts.tokenProgram),
    ],
    data: getMintToPlayerInstructionDataEncoder().encode(
      args as MintToPlayerInstructionDataArgs,
    ),
    programAddress,
  } as MintToPlayerInstruction<
    TProgramAddress,
    TAccountAdmin,
    TAccountUser,
    TAccountPlayerUser,
    TAccountGame,
    TAccountMint,
    TAccountPlayerAta,
    TAccountTokenProgram
  >);
}

export type MintToPlayerInput<
  TAccountAdmin extends string = string,
  TAccountUser extends string = string,
  TAccountPlayerUser extends string = string,
  TAccountGame extends string = string,
  TAccountMint extends string = string,
  TAccountPlayerAta extends string = string,
  TAccountTokenProgram extends string = string,
> = {
  admin: TransactionSigner<TAccountAdmin>;
  user: Address<TAccountUser>;
  playerUser: Address<TAccountPlayerUser>;
  game: Address<TAccountGame>;
  mint: Address<TAccountMint>;
  /**
   * `mint`. Its address is validated against the deterministic ATA
   * derivation for `(player_user, mint)` in the handler, and its
   * initialized state is checked explicitly there too — it must already
   * exist, since depositing never auto-joins a player — mirroring
   * `JoinGame`'s own `player_ata` validation approach.
   */
  playerAta: Address<TAccountPlayerAta>;
  tokenProgram?: Address<TAccountTokenProgram>;
  gameId: MintToPlayerInstructionDataArgs["gameId"];
  username: MintToPlayerInstructionDataArgs["username"];
  playerUsername: MintToPlayerInstructionDataArgs["playerUsername"];
  amount: MintToPlayerInstructionDataArgs["amount"];
};

export function getMintToPlayerInstruction<
  TAccountAdmin extends string,
  TAccountUser extends string,
  TAccountPlayerUser extends string,
  TAccountGame extends string,
  TAccountMint extends string,
  TAccountPlayerAta extends string,
  TAccountTokenProgram extends string,
  TProgramAddress extends Address = typeof GAME_TOKEN_WALLET_PROGRAM_ADDRESS,
>(
  input: MintToPlayerInput<
    TAccountAdmin,
    TAccountUser,
    TAccountPlayerUser,
    TAccountGame,
    TAccountMint,
    TAccountPlayerAta,
    TAccountTokenProgram
  >,
  config?: { programAddress?: TProgramAddress },
): MintToPlayerInstruction<
  TProgramAddress,
  TAccountAdmin,
  TAccountUser,
  TAccountPlayerUser,
  TAccountGame,
  TAccountMint,
  TAccountPlayerAta,
  TAccountTokenProgram
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? GAME_TOKEN_WALLET_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    admin: { value: input.admin ?? null, isWritable: true },
    user: { value: input.user ?? null, isWritable: false },
    playerUser: { value: input.playerUser ?? null, isWritable: false },
    game: { value: input.game ?? null, isWritable: false },
    mint: { value: input.mint ?? null, isWritable: true },
    playerAta: { value: input.playerAta ?? null, isWritable: true },
    tokenProgram: { value: input.tokenProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.tokenProgram.value) {
    accounts.tokenProgram.value =
      "TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA" as Address<"TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.admin),
      getAccountMeta(accounts.user),
      getAccountMeta(accounts.playerUser),
      getAccountMeta(accounts.game),
      getAccountMeta(accounts.mint),
      getAccountMeta(accounts.playerAta),
      getAccountMeta(accounts.tokenProgram),
    ],
    data: getMintToPlayerInstructionDataEncoder().encode(
      args as MintToPlayerInstructionDataArgs,
    ),
    programAddress,
  } as MintToPlayerInstruction<
    TProgramAddress,
    TAccountAdmin,
    TAccountUser,
    TAccountPlayerUser,
    TAccountGame,
    TAccountMint,
    TAccountPlayerAta,
    TAccountTokenProgram
  >);
}

export type ParsedMintToPlayerInstruction<
  TProgram extends string = typeof GAME_TOKEN_WALLET_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    admin: TAccountMetas[0];
    user: TAccountMetas[1];
    playerUser: TAccountMetas[2];
    game: TAccountMetas[3];
    mint: TAccountMetas[4];
    /**
     * `mint`. Its address is validated against the deterministic ATA
     * derivation for `(player_user, mint)` in the handler, and its
     * initialized state is checked explicitly there too — it must already
     * exist, since depositing never auto-joins a player — mirroring
     * `JoinGame`'s own `player_ata` validation approach.
     */
    playerAta: TAccountMetas[5];
    tokenProgram: TAccountMetas[6];
  };
  data: MintToPlayerInstructionData;
};

export function parseMintToPlayerInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedMintToPlayerInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 7) {
    // TODO: Coded error.
    throw new Error("Not enough accounts");
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      admin: getNextAccount(),
      user: getNextAccount(),
      playerUser: getNextAccount(),
      game: getNextAccount(),
      mint: getNextAccount(),
      playerAta: getNextAccount(),
      tokenProgram: getNextAccount(),
    },
    data: getMintToPlayerInstructionDataDecoder().decode(instruction.data),
  };
}
