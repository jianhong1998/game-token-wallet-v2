/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  addDecoderSizePrefix,
  addEncoderSizePrefix,
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  getU32Decoder,
  getU32Encoder,
  getUtf8Decoder,
  getUtf8Encoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type Codec,
  type Decoder,
  type Encoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import {
  findGamePda,
  findMintPda,
  findPlayerUserPda,
  findUserPda,
} from "../pdas";
import { GAME_TOKEN_WALLET_PROGRAM_ADDRESS } from "../programs";
import {
  expectAddress,
  expectSome,
  getAccountMetaFactory,
  type ResolvedAccount,
} from "../shared";

export const CLOSE_GAME_PLAYER_DISCRIMINATOR = new Uint8Array([
  76, 54, 174, 159, 178, 6, 13, 35,
]);

export function getCloseGamePlayerDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    CLOSE_GAME_PLAYER_DISCRIMINATOR,
  );
}

export type CloseGamePlayerInstruction<
  TProgram extends string = typeof GAME_TOKEN_WALLET_PROGRAM_ADDRESS,
  TAccountAdmin extends string | AccountMeta<string> = string,
  TAccountUser extends string | AccountMeta<string> = string,
  TAccountPlayerUser extends string | AccountMeta<string> = string,
  TAccountGame extends string | AccountMeta<string> = string,
  TAccountMint extends string | AccountMeta<string> = string,
  TAccountPlayerAta extends string | AccountMeta<string> = string,
  TAccountTokenProgram extends string | AccountMeta<string> =
    "TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA",
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountAdmin extends string
        ? WritableSignerAccount<TAccountAdmin> &
            AccountSignerMeta<TAccountAdmin>
        : TAccountAdmin,
      TAccountUser extends string
        ? ReadonlyAccount<TAccountUser>
        : TAccountUser,
      TAccountPlayerUser extends string
        ? ReadonlyAccount<TAccountPlayerUser>
        : TAccountPlayerUser,
      TAccountGame extends string
        ? WritableAccount<TAccountGame>
        : TAccountGame,
      TAccountMint extends string
        ? WritableAccount<TAccountMint>
        : TAccountMint,
      TAccountPlayerAta extends string
        ? WritableAccount<TAccountPlayerAta>
        : TAccountPlayerAta,
      TAccountTokenProgram extends string
        ? ReadonlyAccount<TAccountTokenProgram>
        : TAccountTokenProgram,
      ...TRemainingAccounts,
    ]
  >;

export type CloseGamePlayerInstructionData = {
  discriminator: ReadonlyUint8Array;
  gameId: ReadonlyUint8Array;
  username: string;
  playerUsername: string;
};

export type CloseGamePlayerInstructionDataArgs = {
  gameId: ReadonlyUint8Array;
  username: string;
  playerUsername: string;
};

export function getCloseGamePlayerInstructionDataEncoder(): Encoder<CloseGamePlayerInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["gameId", fixEncoderSize(getBytesEncoder(), 16)],
      ["username", addEncoderSizePrefix(getUtf8Encoder(), getU32Encoder())],
      [
        "playerUsername",
        addEncoderSizePrefix(getUtf8Encoder(), getU32Encoder()),
      ],
    ]),
    (value) => ({ ...value, discriminator: CLOSE_GAME_PLAYER_DISCRIMINATOR }),
  );
}

export function getCloseGamePlayerInstructionDataDecoder(): Decoder<CloseGamePlayerInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["gameId", fixDecoderSize(getBytesDecoder(), 16)],
    ["username", addDecoderSizePrefix(getUtf8Decoder(), getU32Decoder())],
    ["playerUsername", addDecoderSizePrefix(getUtf8Decoder(), getU32Decoder())],
  ]);
}

export function getCloseGamePlayerInstructionDataCodec(): Codec<
  CloseGamePlayerInstructionDataArgs,
  CloseGamePlayerInstructionData
> {
  return combineCodec(
    getCloseGamePlayerInstructionDataEncoder(),
    getCloseGamePlayerInstructionDataDecoder(),
  );
}

export type CloseGamePlayerAsyncInput<
  TAccountAdmin extends string = string,
  TAccountUser extends string = string,
  TAccountPlayerUser extends string = string,
  TAccountGame extends string = string,
  TAccountMint extends string = string,
  TAccountPlayerAta extends string = string,
  TAccountTokenProgram extends string = string,
> = {
  admin: TransactionSigner<TAccountAdmin>;
  user?: Address<TAccountUser>;
  playerUser?: Address<TAccountPlayerUser>;
  game?: Address<TAccountGame>;
  mint?: Address<TAccountMint>;
  /**
   * address is validated against the deterministic ATA derivation for
   * `(player_user, mint)` in the handler, and its initialized state is
   * checked explicitly there too — same posture as `quit_game.rs`'s own
   * `player_ata` check.
   */
  playerAta: Address<TAccountPlayerAta>;
  tokenProgram?: Address<TAccountTokenProgram>;
  gameId: CloseGamePlayerInstructionDataArgs["gameId"];
  username: CloseGamePlayerInstructionDataArgs["username"];
  playerUsername: CloseGamePlayerInstructionDataArgs["playerUsername"];
};

export async function getCloseGamePlayerInstructionAsync<
  TAccountAdmin extends string,
  TAccountUser extends string,
  TAccountPlayerUser extends string,
  TAccountGame extends string,
  TAccountMint extends string,
  TAccountPlayerAta extends string,
  TAccountTokenProgram extends string,
  TProgramAddress extends Address = typeof GAME_TOKEN_WALLET_PROGRAM_ADDRESS,
>(
  input: CloseGamePlayerAsyncInput<
    TAccountAdmin,
    TAccountUser,
    TAccountPlayerUser,
    TAccountGame,
    TAccountMint,
    TAccountPlayerAta,
    TAccountTokenProgram
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  CloseGamePlayerInstruction<
    TProgramAddress,
    TAccountAdmin,
    TAccountUser,
    TAccountPlayerUser,
    TAccountGame,
    TAccountMint,
    TAccountPlayerAta,
    TAccountTokenProgram
  >
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? GAME_TOKEN_WALLET_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    admin: { value: input.admin ?? null, isWritable: true },
    user: { value: input.user ?? null, isWritable: false },
    playerUser: { value: input.playerUser ?? null, isWritable: false },
    game: { value: input.game ?? null, isWritable: true },
    mint: { value: input.mint ?? null, isWritable: true },
    playerAta: { value: input.playerAta ?? null, isWritable: true },
    tokenProgram: { value: input.tokenProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.user.value) {
    accounts.user.value = await findUserPda({
      username: expectSome(args.username),
      admin: expectAddress(accounts.admin.value),
    });
  }
  if (!accounts.playerUser.value) {
    accounts.playerUser.value = await findPlayerUserPda({
      playerUsername: expectSome(args.playerUsername),
      admin: expectAddress(accounts.admin.value),
    });
  }
  if (!accounts.game.value) {
    accounts.game.value = await findGamePda({
      gameId: expectSome(args.gameId),
    });
  }
  if (!accounts.mint.value) {
    accounts.mint.value = await findMintPda({
      game: expectAddress(accounts.game.value),
    });
  }
  if (!accounts.tokenProgram.value) {
    accounts.tokenProgram.value =
      "TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA" as Address<"TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.admin),
      getAccountMeta(accounts.user),
      getAccountMeta(accounts.playerUser),
      getAccountMeta(accounts.game),
      getAccountMeta(accounts.mint),
      getAccountMeta(accounts.playerAta),
      getAccountMeta(accounts.tokenProgram),
    ],
    data: getCloseGamePlayerInstructionDataEncoder().encode(
      args as CloseGamePlayerInstructionDataArgs,
    ),
    programAddress,
  } as CloseGamePlayerInstruction<
    TProgramAddress,
    TAccountAdmin,
    TAccountUser,
    TAccountPlayerUser,
    TAccountGame,
    TAccountMint,
    TAccountPlayerAta,
    TAccountTokenProgram
  >);
}

export type CloseGamePlayerInput<
  TAccountAdmin extends string = string,
  TAccountUser extends string = string,
  TAccountPlayerUser extends string = string,
  TAccountGame extends string = string,
  TAccountMint extends string = string,
  TAccountPlayerAta extends string = string,
  TAccountTokenProgram extends string = string,
> = {
  admin: TransactionSigner<TAccountAdmin>;
  user: Address<TAccountUser>;
  playerUser: Address<TAccountPlayerUser>;
  game: Address<TAccountGame>;
  mint: Address<TAccountMint>;
  /**
   * address is validated against the deterministic ATA derivation for
   * `(player_user, mint)` in the handler, and its initialized state is
   * checked explicitly there too — same posture as `quit_game.rs`'s own
   * `player_ata` check.
   */
  playerAta: Address<TAccountPlayerAta>;
  tokenProgram?: Address<TAccountTokenProgram>;
  gameId: CloseGamePlayerInstructionDataArgs["gameId"];
  username: CloseGamePlayerInstructionDataArgs["username"];
  playerUsername: CloseGamePlayerInstructionDataArgs["playerUsername"];
};

export function getCloseGamePlayerInstruction<
  TAccountAdmin extends string,
  TAccountUser extends string,
  TAccountPlayerUser extends string,
  TAccountGame extends string,
  TAccountMint extends string,
  TAccountPlayerAta extends string,
  TAccountTokenProgram extends string,
  TProgramAddress extends Address = typeof GAME_TOKEN_WALLET_PROGRAM_ADDRESS,
>(
  input: CloseGamePlayerInput<
    TAccountAdmin,
    TAccountUser,
    TAccountPlayerUser,
    TAccountGame,
    TAccountMint,
    TAccountPlayerAta,
    TAccountTokenProgram
  >,
  config?: { programAddress?: TProgramAddress },
): CloseGamePlayerInstruction<
  TProgramAddress,
  TAccountAdmin,
  TAccountUser,
  TAccountPlayerUser,
  TAccountGame,
  TAccountMint,
  TAccountPlayerAta,
  TAccountTokenProgram
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? GAME_TOKEN_WALLET_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    admin: { value: input.admin ?? null, isWritable: true },
    user: { value: input.user ?? null, isWritable: false },
    playerUser: { value: input.playerUser ?? null, isWritable: false },
    game: { value: input.game ?? null, isWritable: true },
    mint: { value: input.mint ?? null, isWritable: true },
    playerAta: { value: input.playerAta ?? null, isWritable: true },
    tokenProgram: { value: input.tokenProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.tokenProgram.value) {
    accounts.tokenProgram.value =
      "TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA" as Address<"TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.admin),
      getAccountMeta(accounts.user),
      getAccountMeta(accounts.playerUser),
      getAccountMeta(accounts.game),
      getAccountMeta(accounts.mint),
      getAccountMeta(accounts.playerAta),
      getAccountMeta(accounts.tokenProgram),
    ],
    data: getCloseGamePlayerInstructionDataEncoder().encode(
      args as CloseGamePlayerInstructionDataArgs,
    ),
    programAddress,
  } as CloseGamePlayerInstruction<
    TProgramAddress,
    TAccountAdmin,
    TAccountUser,
    TAccountPlayerUser,
    TAccountGame,
    TAccountMint,
    TAccountPlayerAta,
    TAccountTokenProgram
  >);
}

export type ParsedCloseGamePlayerInstruction<
  TProgram extends string = typeof GAME_TOKEN_WALLET_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    admin: TAccountMetas[0];
    user: TAccountMetas[1];
    playerUser: TAccountMetas[2];
    game: TAccountMetas[3];
    mint: TAccountMetas[4];
    /**
     * address is validated against the deterministic ATA derivation for
     * `(player_user, mint)` in the handler, and its initialized state is
     * checked explicitly there too — same posture as `quit_game.rs`'s own
     * `player_ata` check.
     */
    playerAta: TAccountMetas[5];
    tokenProgram: TAccountMetas[6];
  };
  data: CloseGamePlayerInstructionData;
};

export function parseCloseGamePlayerInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedCloseGamePlayerInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 7) {
    // TODO: Coded error.
    throw new Error("Not enough accounts");
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      admin: getNextAccount(),
      user: getNextAccount(),
      playerUser: getNextAccount(),
      game: getNextAccount(),
      mint: getNextAccount(),
      playerAta: getNextAccount(),
      tokenProgram: getNextAccount(),
    },
    data: getCloseGamePlayerInstructionDataDecoder().decode(instruction.data),
  };
}
