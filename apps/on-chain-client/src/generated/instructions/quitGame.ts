/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  addDecoderSizePrefix,
  addEncoderSizePrefix,
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  getU32Decoder,
  getU32Encoder,
  getUtf8Decoder,
  getUtf8Encoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type Codec,
  type Decoder,
  type Encoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import { findGamePda, findMintPda, findUserPda } from "../pdas";
import { GAME_TOKEN_WALLET_PROGRAM_ADDRESS } from "../programs";
import {
  expectAddress,
  expectSome,
  getAccountMetaFactory,
  type ResolvedAccount,
} from "../shared";

export const QUIT_GAME_DISCRIMINATOR = new Uint8Array([
  87, 219, 3, 107, 47, 61, 151, 69,
]);

export function getQuitGameDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(QUIT_GAME_DISCRIMINATOR);
}

export type QuitGameInstruction<
  TProgram extends string = typeof GAME_TOKEN_WALLET_PROGRAM_ADDRESS,
  TAccountAdmin extends string | AccountMeta<string> = string,
  TAccountUser extends string | AccountMeta<string> = string,
  TAccountGame extends string | AccountMeta<string> = string,
  TAccountMint extends string | AccountMeta<string> = string,
  TAccountPlayerAta extends string | AccountMeta<string> = string,
  TAccountTokenProgram extends string | AccountMeta<string> =
    "TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA",
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountAdmin extends string
        ? WritableSignerAccount<TAccountAdmin> &
            AccountSignerMeta<TAccountAdmin>
        : TAccountAdmin,
      TAccountUser extends string
        ? ReadonlyAccount<TAccountUser>
        : TAccountUser,
      TAccountGame extends string
        ? WritableAccount<TAccountGame>
        : TAccountGame,
      TAccountMint extends string
        ? WritableAccount<TAccountMint>
        : TAccountMint,
      TAccountPlayerAta extends string
        ? WritableAccount<TAccountPlayerAta>
        : TAccountPlayerAta,
      TAccountTokenProgram extends string
        ? ReadonlyAccount<TAccountTokenProgram>
        : TAccountTokenProgram,
      ...TRemainingAccounts,
    ]
  >;

export type QuitGameInstructionData = {
  discriminator: ReadonlyUint8Array;
  gameId: ReadonlyUint8Array;
  username: string;
};

export type QuitGameInstructionDataArgs = {
  gameId: ReadonlyUint8Array;
  username: string;
};

export function getQuitGameInstructionDataEncoder(): Encoder<QuitGameInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["gameId", fixEncoderSize(getBytesEncoder(), 16)],
      ["username", addEncoderSizePrefix(getUtf8Encoder(), getU32Encoder())],
    ]),
    (value) => ({ ...value, discriminator: QUIT_GAME_DISCRIMINATOR }),
  );
}

export function getQuitGameInstructionDataDecoder(): Decoder<QuitGameInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["gameId", fixDecoderSize(getBytesDecoder(), 16)],
    ["username", addDecoderSizePrefix(getUtf8Decoder(), getU32Decoder())],
  ]);
}

export function getQuitGameInstructionDataCodec(): Codec<
  QuitGameInstructionDataArgs,
  QuitGameInstructionData
> {
  return combineCodec(
    getQuitGameInstructionDataEncoder(),
    getQuitGameInstructionDataDecoder(),
  );
}

export type QuitGameAsyncInput<
  TAccountAdmin extends string = string,
  TAccountUser extends string = string,
  TAccountGame extends string = string,
  TAccountMint extends string = string,
  TAccountPlayerAta extends string = string,
  TAccountTokenProgram extends string = string,
> = {
  admin: TransactionSigner<TAccountAdmin>;
  user?: Address<TAccountUser>;
  game?: Address<TAccountGame>;
  mint?: Address<TAccountMint>;
  /**
   * `mint`. Its address is validated against the deterministic ATA
   * derivation for `(user, mint)` in the handler, and its initialized
   * state is checked explicitly there too — mirroring `JoinGame`'s own
   * `player_ata` validation approach. Kept as `UncheckedAccount` (not a
   * typed `Account<'info, TokenAccount>`) so an uninitialized ATA reports
   * the custom `PlayerNotInGame` error instead of a generic Anchor
   * deserialization failure; the handler manually deserializes it once
   * it's confirmed non-empty, to read the actual balance to burn.
   */
  playerAta: Address<TAccountPlayerAta>;
  tokenProgram?: Address<TAccountTokenProgram>;
  gameId: QuitGameInstructionDataArgs["gameId"];
  username: QuitGameInstructionDataArgs["username"];
};

export async function getQuitGameInstructionAsync<
  TAccountAdmin extends string,
  TAccountUser extends string,
  TAccountGame extends string,
  TAccountMint extends string,
  TAccountPlayerAta extends string,
  TAccountTokenProgram extends string,
  TProgramAddress extends Address = typeof GAME_TOKEN_WALLET_PROGRAM_ADDRESS,
>(
  input: QuitGameAsyncInput<
    TAccountAdmin,
    TAccountUser,
    TAccountGame,
    TAccountMint,
    TAccountPlayerAta,
    TAccountTokenProgram
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  QuitGameInstruction<
    TProgramAddress,
    TAccountAdmin,
    TAccountUser,
    TAccountGame,
    TAccountMint,
    TAccountPlayerAta,
    TAccountTokenProgram
  >
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? GAME_TOKEN_WALLET_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    admin: { value: input.admin ?? null, isWritable: true },
    user: { value: input.user ?? null, isWritable: false },
    game: { value: input.game ?? null, isWritable: true },
    mint: { value: input.mint ?? null, isWritable: true },
    playerAta: { value: input.playerAta ?? null, isWritable: true },
    tokenProgram: { value: input.tokenProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.user.value) {
    accounts.user.value = await findUserPda({
      username: expectSome(args.username),
      admin: expectAddress(accounts.admin.value),
    });
  }
  if (!accounts.game.value) {
    accounts.game.value = await findGamePda({
      gameId: expectSome(args.gameId),
    });
  }
  if (!accounts.mint.value) {
    accounts.mint.value = await findMintPda({
      game: expectAddress(accounts.game.value),
    });
  }
  if (!accounts.tokenProgram.value) {
    accounts.tokenProgram.value =
      "TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA" as Address<"TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.admin),
      getAccountMeta(accounts.user),
      getAccountMeta(accounts.game),
      getAccountMeta(accounts.mint),
      getAccountMeta(accounts.playerAta),
      getAccountMeta(accounts.tokenProgram),
    ],
    data: getQuitGameInstructionDataEncoder().encode(
      args as QuitGameInstructionDataArgs,
    ),
    programAddress,
  } as QuitGameInstruction<
    TProgramAddress,
    TAccountAdmin,
    TAccountUser,
    TAccountGame,
    TAccountMint,
    TAccountPlayerAta,
    TAccountTokenProgram
  >);
}

export type QuitGameInput<
  TAccountAdmin extends string = string,
  TAccountUser extends string = string,
  TAccountGame extends string = string,
  TAccountMint extends string = string,
  TAccountPlayerAta extends string = string,
  TAccountTokenProgram extends string = string,
> = {
  admin: TransactionSigner<TAccountAdmin>;
  user: Address<TAccountUser>;
  game: Address<TAccountGame>;
  mint: Address<TAccountMint>;
  /**
   * `mint`. Its address is validated against the deterministic ATA
   * derivation for `(user, mint)` in the handler, and its initialized
   * state is checked explicitly there too — mirroring `JoinGame`'s own
   * `player_ata` validation approach. Kept as `UncheckedAccount` (not a
   * typed `Account<'info, TokenAccount>`) so an uninitialized ATA reports
   * the custom `PlayerNotInGame` error instead of a generic Anchor
   * deserialization failure; the handler manually deserializes it once
   * it's confirmed non-empty, to read the actual balance to burn.
   */
  playerAta: Address<TAccountPlayerAta>;
  tokenProgram?: Address<TAccountTokenProgram>;
  gameId: QuitGameInstructionDataArgs["gameId"];
  username: QuitGameInstructionDataArgs["username"];
};

export function getQuitGameInstruction<
  TAccountAdmin extends string,
  TAccountUser extends string,
  TAccountGame extends string,
  TAccountMint extends string,
  TAccountPlayerAta extends string,
  TAccountTokenProgram extends string,
  TProgramAddress extends Address = typeof GAME_TOKEN_WALLET_PROGRAM_ADDRESS,
>(
  input: QuitGameInput<
    TAccountAdmin,
    TAccountUser,
    TAccountGame,
    TAccountMint,
    TAccountPlayerAta,
    TAccountTokenProgram
  >,
  config?: { programAddress?: TProgramAddress },
): QuitGameInstruction<
  TProgramAddress,
  TAccountAdmin,
  TAccountUser,
  TAccountGame,
  TAccountMint,
  TAccountPlayerAta,
  TAccountTokenProgram
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? GAME_TOKEN_WALLET_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    admin: { value: input.admin ?? null, isWritable: true },
    user: { value: input.user ?? null, isWritable: false },
    game: { value: input.game ?? null, isWritable: true },
    mint: { value: input.mint ?? null, isWritable: true },
    playerAta: { value: input.playerAta ?? null, isWritable: true },
    tokenProgram: { value: input.tokenProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.tokenProgram.value) {
    accounts.tokenProgram.value =
      "TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA" as Address<"TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.admin),
      getAccountMeta(accounts.user),
      getAccountMeta(accounts.game),
      getAccountMeta(accounts.mint),
      getAccountMeta(accounts.playerAta),
      getAccountMeta(accounts.tokenProgram),
    ],
    data: getQuitGameInstructionDataEncoder().encode(
      args as QuitGameInstructionDataArgs,
    ),
    programAddress,
  } as QuitGameInstruction<
    TProgramAddress,
    TAccountAdmin,
    TAccountUser,
    TAccountGame,
    TAccountMint,
    TAccountPlayerAta,
    TAccountTokenProgram
  >);
}

export type ParsedQuitGameInstruction<
  TProgram extends string = typeof GAME_TOKEN_WALLET_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    admin: TAccountMetas[0];
    user: TAccountMetas[1];
    game: TAccountMetas[2];
    mint: TAccountMetas[3];
    /**
     * `mint`. Its address is validated against the deterministic ATA
     * derivation for `(user, mint)` in the handler, and its initialized
     * state is checked explicitly there too — mirroring `JoinGame`'s own
     * `player_ata` validation approach. Kept as `UncheckedAccount` (not a
     * typed `Account<'info, TokenAccount>`) so an uninitialized ATA reports
     * the custom `PlayerNotInGame` error instead of a generic Anchor
     * deserialization failure; the handler manually deserializes it once
     * it's confirmed non-empty, to read the actual balance to burn.
     */
    playerAta: TAccountMetas[4];
    tokenProgram: TAccountMetas[5];
  };
  data: QuitGameInstructionData;
};

export function parseQuitGameInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedQuitGameInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 6) {
    // TODO: Coded error.
    throw new Error("Not enough accounts");
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      admin: getNextAccount(),
      user: getNextAccount(),
      game: getNextAccount(),
      mint: getNextAccount(),
      playerAta: getNextAccount(),
      tokenProgram: getNextAccount(),
    },
    data: getQuitGameInstructionDataDecoder().decode(instruction.data),
  };
}
